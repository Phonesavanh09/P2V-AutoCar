<!DOCTYPE html>
<html lang="<%- lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- lang === 'la' ? 'ສະເໜີຂາຍລົດ' : 'Sell Your Car' %></title>
    <link href="https://fonts.googleapis.com/css2?family=Phetsarath&family=Prompt:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .lao-text { font-family: 'Phetsarath', sans-serif; }
        .card { border-radius: 10px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/images/logo1.png" alt="Logo" style="height: 80px; margin-right: 15px;">
                <%- lang === 'la' ? 'P2V Laos Auto Car' : 'Used Car Sales' %>
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/"><span class="lao-text"><%- lang === 'la' ? 'ໜ້າຫຼັກ' : 'Home' %></span></a>
                <a class="nav-link active" href="/sell-car"><span class="lao-text"><%- lang === 'la' ? 'ສະເໜີຂາຍລົດ' : 'Sell Your Car' %></span></a>
                <a class="nav-link" href="/reviews"><span class="lao-text"><%- lang === 'la' ? 'ຣີວິວ' : 'Reviews' %></span></a>
                <a class="nav-link" href="/contact"><span class="lao-text"><%- lang === 'la' ? 'ຕິດຕໍ່' : 'Contact' %></span></a>
                <a class="nav-link" href="?lang=<%- lang === 'la' ? 'en' : 'la' %>" class="lao-text"><%- lang === 'la' ? 'English' : 'ລາວ' %></a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h2 class="lao-text"><%- lang === 'la' ? 'ສະເໜີຂາຍລົດ' : 'Sell Your Car' %></h2>
            </div>
            <div class="card-body">
                <% if (error) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <%- error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                 <% if (typeof success !== 'undefined' && success) { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <%- req.query.success %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                <form action="/sell-car" method="post" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label class="form-label lao-text"><%- lang === 'la' ? 'ຍີ່ຫໍ້' : 'Brand' %></label>
                        <input type="text" name="brand" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label lao-text"><%- lang === 'la' ? 'ລຸ້ນ' : 'Model' %></label>
                        <input type="text" name="model" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label lao-text"><%- lang === 'la' ? 'ປີ' : 'Year' %></label>
                        <input type="number" name="year" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label lao-text"><%- lang === 'la' ? 'ລາຄາ' : 'Price' %></label>
                        <input type="number" name="price" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label lao-text"><%- lang === 'la' ? 'ເກຍ' : 'Transmission' %></label>
                        <select name="transmission" class="form-control" required>
                            <option value="Automatic"><%- lang === 'la' ? 'ອັດຕະໂນມັດ' : 'Automatic' %></option>
                            <option value="Manual"><%- lang === 'la' ? 'ກະປຸກ' : 'Manual' %></option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label lao-text"><%- lang === 'la' ? 'ຕິດຕໍ່' : 'Contact' %></label>
                        <input type="text" name="contact" class="form-control" placeholder="<%- lang === 'la' ? 'ເບີໂທຫຼືອີເມວ' : 'Phone or Email' %>" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label lao-text"><%- lang === 'la' ? 'ແຂວງ' : 'Province' %></label>
                        <select name="province" class="form-control" required>
                            <% provinces.forEach(province => { %>
                                <option value="<%- province %>"><%- province %></option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label lao-text"><%- lang === 'la' ? 'ຮູບພາບ (ສູງສຸດ 10 ຮູບ)' : 'Images (up to 10)' %></label>
                        <input type="file" name="images" class="form-control" multiple>
                    </div>
                    <button type="submit" class="btn btn-primary lao-text"><%- lang === 'la' ? 'ສົ່ງຄຳສະເໜີ' : 'Submit Request' %></button>
                    <a href="/" class="btn btn-secondary ms-2 lao-text"><%- lang === 'la' ? 'ກັບຄືນ' : 'Back' %></a>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>