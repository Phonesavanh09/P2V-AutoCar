<!DOCTYPE html>
<html lang="lo">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= car.name %> - PV Auto Car</title>
<link rel="stylesheet" href="/css/style.css">
<script src="/js/script.js" defer></script>
</head>
<body>
<%- include('partials/header', { lang: lang }) %>

<main>
<h1><%= car.name %></h1>

<div class="gallery">
<% car.images.forEach(img => { %>
<img src="<%= img %>" alt="<%= car.name %>" onclick="openLightbox(this.src)">
<% }) %>
</div>

<ul class="car-details">
<li><strong>Brand:</strong> <%= car.brand %></li>
<li><strong>Year:</strong> <%= car.year %></li>
<li><strong>Price:</strong> $<%= car.price %></li>
<li><strong>Mileage:</strong> <%= car.mileage %></li>
<li><strong>Fuel:</strong> <%= car.fuel %></li>
<li><strong>Transmission:</strong> <%= car.transmission %></li>
<li><strong>Color:</strong> <%= car.color %></li>
<li><strong>Status:</strong> <span class="status <%= car.status %>"><%= car.status %></span></li>
<li><strong>Date Added:</strong> <%= new Date(car.dateAdded).toLocaleDateString() %></li>
</ul>

<div id="lightbox" onclick="closeLightbox()"><img id="lightbox-img" src=""></div>

</main>
<%- include('partials/footer', { lang: lang }) %>
</body>
</html>