<!DOCTYPE html>
<html lang="<%- lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- lang === 'la' ? 'ລາຍລະອຽດລົດ' : 'Car Detail' %></title>
    <link href="https://fonts.googleapis.com/css2?family=Phetsarath&family=Prompt:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .lao-text { font-family: 'Phetsarath', sans-serif; }
        .card { border-radius: 10px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); }
        .car-image { width: 100%; height: 300px; object-fit: contain; border-top-left-radius: 10px; border-top-right-radius: 10px; }
        .gallery img { max-width: 150px; height: auto; margin: 5px; cursor: pointer; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/images/logo.png" alt="Logo" style="height: 50px; margin-right: 10px;">
                <%- lang === 'la' ? 'ຂາຍລົດມືສອງ' : 'Used Car Sales' %>
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/"><span class="lao-text"><%- lang === 'la' ? 'ໜ້າຫຼັກ' : 'Home' %></span></a>
                <a class="nav-link" href="/sell-car"><span class="lao-text"><%- lang === 'la' ? 'ສະເໜີຂາຍລົດ' : 'Sell Your Car' %></span></a>
                <a class="nav-link" href="/reviews"><span class="lao-text"><%- lang === 'la' ? 'ຣີວິວ' : 'Reviews' %></span></a>
                <a class="nav-link" href="/contact"><span class="lao-text"><%- lang === 'la' ? 'ຕິດຕໍ່' : 'Contact' %></span></a>
                <a class="nav-link" href="?lang=<%- lang === 'la' ? 'en' : 'la' %>" class="lao-text"><%- lang === 'la' ? 'English' : 'ລາວ' %></a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h2 class="lao-text"><%- lang === 'la' ? 'ລາຍລະອຽດລົດ' : 'Car Detail' %></h2>
            </div>
            <div class="card-body">
                <% if (car) { %>
                    <div class="gallery mb-3">
                        <% if (car.images && car.images.length > 0) { %>
                            <% car.images.forEach(image => { %>
                                <img src="<%- image %>" alt="Car Image" onclick="document.getElementById('mainImage').src=this.src">
                            <% }) %>
                        <% } else { %>
                            <img src="/images/placeholder.jpg" alt="Placeholder" id="mainImage">
                        <% } %>
                    </div>
                    <img src="<%- car.coverImage || (car.images && car.images.length > 0 ? car.images[0] : '/images/placeholder.jpg') %>" alt="Main Car Image" class="car-image" id="mainImage">
                    <h4 class="lao-text mt-3"><%- lang === 'la' ? 'ຍີ່ຫໍ້' : 'Brand' %>: <%= car.brand %></h4>
                    <h4 class="lao-text"><%- lang === 'la' ? 'ລຸ້ນ' : 'Model' %>: <%= car.model %></h4>
                    <p class="lao-text"><%- lang === 'la' ? 'ປີ' : 'Year' %>: <%= car.year %></p>
                    <p class="lao-text"><%- lang === 'la' ? 'ລາຄາ' : 'Price' %>: <%= car.price.toLocaleString() %> LAK</p>
                    <p class="lao-text">
                        <%- lang === 'la' ? 'ເກຍ' : 'Transmission' %>:
                        <% if (lang === 'la') { %>
                            <%= car.transmission === 'Automatic' ? 'ອໍໂຕ້' : (car.transmission === 'Manual' ? 'ກະປຸກ' : car.transmission) %>
                        <% } else { %>
                            <%= car.transmission %>
                        <% } %>
                    </p>
                    <p class="lao-text"><%- lang === 'la' ? 'ແຂວງ' : 'Province' %>: <%= car.province %></p>
                    <p class="lao-text"><%- lang === 'la' ? 'ລາຍລະອຽດ' : 'Description' %>: <%= car.description || 'ไม่มีรายละเอียด' %></p>
                    <h5 class="lao-text mt-4"><%- lang === 'la' ? 'ຕິດຕໍ່ພວກເຮົາ' : 'Contact Us' %></h5>
                    <p class="lao-text"><%- lang === 'la' ? 'ເບີໂທ: +856 20 9992 3997' : 'Phone: +856 20 9992 3997' %></p>
                    <p class="lao-text"><%- lang === 'la' ? 'ອີເມວ: p2v.autocar@gmail.com' : 'Email: p2v.autocar@gmail.com' %></p>
                    <a href="/" class="btn btn-primary mt-3 lao-text"><%- lang === 'la' ? 'ກັບຄືນ' : 'Back' %></a>
                <% } else { %>
                    <p class="lao-text alert alert-warning"><%- lang === 'la' ? 'ບໍ່ພົບລົດ' : 'Car not found' %></p>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>