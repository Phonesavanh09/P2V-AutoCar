<!DOCTYPE html>
<html lang="lo">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>
<% if(lang==='lo'){ %>ຕິດຕໍ່<% } %>
<% if(lang==='en'){ %>Contact<% } %>
<% if(lang==='cn'){ %>联系<% } %>
</title>
<link rel="stylesheet" href="/css/style.css">
</head>
<body>
<%- include('partials/header', { lang: lang }) %>

<main>
<h1>
<% if(lang==='lo'){ %>ຕິດຕໍ່<% } %>
<% if(lang==='en'){ %>Contact<% } %>
<% if(lang==='cn'){ %>联系<% } %>
</h1>

<form action="mailto:pvautocar@example.com" method="POST">
<input type="text" name="name" placeholder="<%= lang==='lo' ? 'ຊື່' : lang==='en' ? 'Name' : '姓名' %>" required>
<input type="email" name="email" placeholder="Email" required>
<textarea name="message" placeholder="<%= lang==='lo' ? 'ຂໍ້ຄວາມ' : lang==='en' ? 'Message' : '留言' %>" required></textarea>
<button type="submit">
<% if(lang==='lo'){ %>ສົ່ງ<% } %>
<% if(lang==='en'){ %>Send<% } %>
<% if(lang==='cn'){ %>发送<% } %>
</button>
</form>
</main>

<%- include('partials/footer', { lang: lang }) %>
</body>
</html>