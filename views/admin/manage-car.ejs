<!DOCTYPE html>
<html lang="<%- typeof lang !== 'undefined' ? lang : 'en' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof lang !== 'undefined' && lang === 'la' ? 'ຈັດການລົດ' : 'Manage Cars' %></title>
    <link href="https://fonts.googleapis.com/css2?family=Phetsarath&family=Prompt:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .lao-text { font-family: 'Phetsarath', sans-serif; }
        .card { border-radius: 10px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); }
        .car-image { max-width: 100px; height: auto; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/admin/dashboard"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ແຜງຄວບຄຸມ' : 'Admin Dashboard' %></a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link active" href="/admin/manage-car"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ຈັດການລົດ' : 'Manage Cars' %></a>
                <a class="nav-link" href="/admin/add-car"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ເພີ່ມລົດ' : 'Add Car' %></a>
                <a class="nav-link" href="/"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ໜ້າຫຼັງ' : 'Home' %></a>
                <a class="nav-link lao-text" href="?lang=<%- typeof lang !== 'undefined' && lang === 'la' ? 'en' : 'la' %>"><%- typeof lang !== 'undefined' && lang === 'la' ? 'English' : 'ລາວ' %></a>
                <a class="nav-link text-danger" href="/admin/logout" onclick="return confirm(`<%- typeof lang !== 'undefined' && lang === 'la' ? 'ທ່ານແນ່ໃຈບໍ່ທີ່ຈະອອກຈາກລະບົບ?' : 'Are you sure you want to logout?' %>`);"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ອອກຈາກລະບົບ' : 'Logout' %></a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h2 class="lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ຈັດການລົດ' : 'Manage Cars' %></h2>
            </div>
            <div class="card-body">
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <%- error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                <% if (typeof success !== 'undefined' && success) { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <%- success %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                <div class="mb-3">
                    <label class="lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ຕິດຕໍ່' : 'Filter by Province' %>:</label>
                    <select id="provinceFilter" class="form-control w-25 d-inline-block" onchange="filterCars()">
                        <option value="" <%- (typeof province !== 'undefined' && province === '') || typeof province === 'undefined' ? 'selected' : '' %>><%- typeof lang !== 'undefined' && lang === 'la' ? 'ທັງໝົດ' : 'All' %></option>
                        <% if (typeof provinces !== 'undefined' && Array.isArray(provinces)) { %>
                            <% provinces.forEach(province => { %>
                                <option value="<%- province %>"><%- province %></option>
                            <% }) %>
                        <% } %>
                    </select>
                    <label class="lao-text ms-3"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ຈັດລຳດັບຕາມລາຄາ' : 'Sort by Price' %>:</label>
                    <select id="priceSort" class="form-control w-25 d-inline-block" onchange="sortCars()">
                        <option value="default" <%- typeof sort !== 'undefined' && sort === 'default' ? 'selected' : '' %>><%- typeof lang !== 'undefined' && lang === 'la' ? 'ເລີຍຕາມວັນທີ່' : 'By Date' %></option>
                        <option value="price_asc" <%- typeof sort !== 'undefined' && sort === 'price_asc' ? 'selected' : '' %>><%- typeof lang !== 'undefined' && lang === 'la' ? 'ຈາກຖືກໄປແພງ' : 'Low to High' %></option>
                        <option value="price_desc" <%- typeof sort !== 'undefined' && sort === 'price_desc' ? 'selected' : '' %>><%- typeof lang !== 'undefined' && lang === 'la' ? 'ຈາກແພງໄປຖືກ' : 'High to Low' %></option>
                    </select>
                </div>
                <% if (cars.length > 0) { %>
                    <div id="carList">
                        <% cars.forEach(car => { %>
                            <div class="card mb-3">
                                <div class="card-body row">
                                    <div class="col-md-2">
                                        <% if (car.coverImage || (car.images && car.images.length > 0)) { %>
                                            <img src="<%- car.coverImage || car.images[0] %>" alt="Car Image" class="car-image">
                                        <% } else { %>
                                            <img src="/images/placeholder.jpg" alt="Placeholder" class="car-image">
                                        <% } %>
                                    </div>
                                    <div class="col-md-7">
                                        <h5 class="card-title lao-text"><%- car.brand %> <%- car.model %></h5>
                                        <p class="card-text lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ປີ' : 'Year' %>: <%- car.year %></p>
                                        <p class="card-text lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ລາຄາ' : 'Price' %>: <%- car.price.toLocaleString() %> LAK</p>
                                        <p class="card-text lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ຈຸງຮ້ອງ' : 'Province' %>: <%- car.province %></p>
                                        <p class="card-text lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ວັນທີ່ໂພສ' : 'Posted Date' %>: <%- new Date(car.postedDate).toLocaleDateString() %></p>
                                        <p class="card-text lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ສະຖານະ' : 'Status' %>: <%- car.status === 'available' ? (typeof lang !== 'undefined' && lang === 'la' ? 'ມີສິນຄ້າ' : 'Available') : (typeof lang !== 'undefined' && lang === 'la' ? 'ຂາຍແລ້ວ' : 'Sold') %></p>
                                    </div>
                                    <div class="col-md-3">
                                        <a href="/admin/edit-car/<%- car._id %>" class="btn btn-warning btn-sm lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ແກ້ໄຂ' : 'Edit' %></a>
                                        <a href="/admin/delete-car/<%- car._id %>" class="btn btn-danger btn-sm ms-2 lao-text" onclick="return confirm(`<%- typeof lang !== 'undefined' && lang === 'la' ? 'ທ່ານແນ່ໃຈບໍ່ທີ່ຈະລົບ?' : 'Are you sure you want to delete?' %>`);"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ລົບ' : 'Delete' %></a>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <p class="text-center alert alert-warning lao-text"><%- lang === 'la' ? 'ບໍ່ມີລົດ' : 'No cars' %></p>
                <% } %>
                <a href="/admin/dashboard" class="btn btn-secondary mt-3 lao-text"><%- typeof lang !== 'undefined' && lang === 'la' ? 'ກັບຄືນ' : 'Back' %></a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function filterCars() {
            let province = document.getElementById("provinceFilter").value;
            let sort = document.getElementById("priceSort").value;
            window.location.href = `/admin/manage-car?province=${province}&sort=${sort}`;
        }

        function sortCars() {
            let province = document.getElementById("provinceFilter").value;
            let sort = document.getElementById("priceSort").value;
            window.location.href = `/admin/manage-car?province=${province}&sort=${sort}`;
        }
    </script>
</body>
</html>