<!DOCTYPE html>
<html lang="lo">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>
<% if(lang==='lo'){ %>ເຂົ້າລະບົບຜູ້ດູແລ<% } %>
<% if(lang==='en'){ %>Admin Login<% } %>
<% if(lang==='cn'){ %>管理员登录<% } %>
</title>
<link rel="stylesheet" href="/css/style.css">
</head>
<body>
<main style="max-width:400px;margin:auto;padding:50px;">
<h1>
<% if(lang==='lo'){ %>ເຂົ້າລະບົບຜູ້ດູແລ<% } %>
<% if(lang==='en'){ %>Admin Login<% } %>
<% if(lang==='cn'){ %>管理员登录<% } %>
</h1>
<form action="/admin/login" method="POST">
<input type="password" name="password" placeholder="<%= lang==='lo' ? 'ລະຫັດຜ່ານ' : lang==='en' ? 'Password' : '密码' %>" required>
<button type="submit">
<% if(lang==='lo'){ %>ເຂົ້າລະບົບ<% } %>
<% if(lang==='en'){ %>Login<% } %>
<% if(lang==='cn'){ %>登录<% } %>
</button>
</form>
</main>
</body>
</html>