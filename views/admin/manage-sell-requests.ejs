<!DOCTYPE html>
<html lang="lo">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ຈັດການຄຳຂໍຂາຍລົດ</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Lao:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/admin/dashboard">ແຜງຄວບຄຸມ</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/admin/manage-car">ຈັດການລົດ</a>
                <a class="nav-link" href="/admin/manage-sell-requests">ຈັດການຄຳຂໍຂາຍ</a>
                <a class="nav-link" href="/">ໜ້າຫຼັກ</a>
                <a class="nav-link text-danger" href="/admin/logout" onclick="return confirm('ທ່ານແນ່ໃຈບໍ່ທີ່ຈະອອກຈາກລະບົບ?');">ອອກຈາກລະບົບ</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h2>ຈັດການຄຳຂໍຂາຍລົດ</h2>
        <table class="table table-striped bg-dark text-light">
            <thead>
                <tr>
                    <th>ຍີ່ຫໍ້</th>
                    <th>ລຸ້ນ</th>
                    <th>ປີ</th>
                    <th>ລາຄາ</th>
                    <th>ຜູ້ຂາຍ</th>
                    <th>ສະຖານະ</th>
                    <th>ການກະທຳ</th>
                </tr>
            </thead>
            <tbody>
                <% sellRequests.forEach(request => { %>
                    <tr>
                        <td><%- request.brand %></td>
                        <td><%- request.model %></td>
                        <td><%- request.year %></td>
                        <td><%- request.price.toLocaleString() %> ກີບ</td>
                        <td><%- request.sellerName %> (<%- request.phone %>)</td>
                        <td><%- request.status === 'pending' ? 'ລໍຖ້າການດຳເນີນການ' : request.status === 'approved' ? 'ອະນຸມັດ' : 'ປະຕິເສດ' %></td>
                        <td>
                            <% if (request.status === 'pending') { %>
                                <form action="/admin/approve-sell-request/<%- request._id %>" method="post" style="display: inline;">
                                    <button type="submit" class="btn btn-success btn-sm">ອະນຸມັດ</button>
                                </form>
                                <form action="/admin/reject-sell-request/<%- request._id %>" method="post" style="display: inline;">
                                    <button type="submit" class="btn btn-danger btn-sm">ປະຕິເສດ</button>
                                </form>
                            <% } %>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>