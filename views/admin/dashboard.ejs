<!DOCTYPE html>
<html lang="lo">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Admin Dashboard</title>
<link rel="stylesheet" href="/css/style.css">
</head>
<body>
<header>
<h1>Admin Dashboard</h1>
<nav>
<a href="/admin/add-car"><% if(lang==='lo'){ %>ເພີ່ມລົດ<% } %><% if(lang==='en'){ %>Add Car<% } %><% if(lang==='cn'){ %>添加车辆<% } %></a> |
<a href="/admin/logout"><% if(lang==='lo'){ %>ອອກຈາກລະບົບ<% } %><% if(lang==='en'){ %>Logout<% } %><% if(lang==='cn'){ %>退出<% } %></a>
</nav>
</header>

<main>
<h2>All Cars</h2>
<table>
<tr>
<th>Name</th><th>Brand</th><th>Year</th><th>Price</th><th>Status</th><th>Actions</th>
</tr>
<% cars.forEach(car => { %>
<tr>
<td><%= car.name %></td>
<td><%= car.brand %></td>
<td><%= car.year %></td>
<td>$<%= car.price %></td>
<td class="status <%= car.status %>"><%= car.status %></td>
<td>
<a href="/admin/edit-car/<%= car._id %>"><% if(lang==='lo'){ %>ແກ້ໄຂ<% } %><% if(lang==='en'){ %>Edit<% } %><% if(lang==='cn'){ %>编辑<% } %></a> |
<a href="/admin/delete-car/<%= car._id %>" onclick="return confirm('Are you sure?')">
<% if(lang==='lo'){ %>ລຶບ<% } %><% if(lang==='en'){ %>Delete<% } %><% if(lang==='cn'){ %>删除<% } %></a>
</td>
</tr>
<% }) %>
</table>
</main>
</body>
</html>