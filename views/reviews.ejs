<!DOCTYPE html>
<html lang="lo">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>
<% if(lang==='lo'){ %>ຄຳຄິດເຫັນ<% } %>
<% if(lang==='en'){ %>Reviews<% } %>
<% if(lang==='cn'){ %>评论<% } %>
</title>
<link rel="stylesheet" href="/css/style.css">
</head>
<body>
<%- include('partials/header', { lang: lang }) %>

<main>
<h1>
<% if(lang==='lo'){ %>ຄຳຄິດເຫັນ<% } %>
<% if(lang==='en'){ %>Reviews<% } %>
<% if(lang==='cn'){ %>评论<% } %>
</h1>

<ul class="reviews-list">
<% reviews.forEach(r => { %>
<li>
<strong><%= r.name %></strong> (<%= new Date(r.date).toLocaleDateString() %>)<br>
<%= r.message %> | Rating: <%= r.rating %>/5
</li>
<% }) %>
</ul>

<h2>
<% if(lang==='lo'){ %>ເພີ່ມຄຳຄິດເຫັນ<% } %>
<% if(lang==='en'){ %>Add Review<% } %>
<% if(lang==='cn'){ %>添加评论<% } %>
</h2>
<form action="/reviews" method="POST">
<input type="text" name="name" placeholder="<%= lang==='lo' ? 'ຊື່' : lang==='en' ? 'Name' : '姓名' %>" required>
<textarea name="message" placeholder="<%= lang==='lo' ? 'ຄຳຄິດເຫັນ' : lang==='en' ? 'Message' : '留言' %>" required></textarea>
<input type="number" name="rating" placeholder="Rating 1-5" min="1" max="5" required>
<button type="submit">
<% if(lang==='lo'){ %>ສົ່ງ<% } %>
<% if(lang==='en'){ %>Submit<% } %>
<% if(lang==='cn'){ %>提交<% } %>
</button>
</form>
</main>

<%- include('partials/footer', { lang: lang }) %>
</body>
</html>