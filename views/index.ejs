<!DOCTYPE html>
<html lang="lo">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>
<% if(lang==='lo'){ %>PV Auto Car ຂາຍລົດມືສອງ<% } %>
<% if(lang==='en'){ %>PV Auto Car Used Cars<% } %>
<% if(lang==='cn'){ %>PV二手车<% } %>
</title>
<link rel="stylesheet" href="/css/style.css">
<script src="/js/script.js" defer></script>
</head>
<body>
<%- include('partials/header', { lang: lang }) %>

<main>
<h2>
<% if(lang==='lo'){ %>ລາຍການລົດມືສອງ<% } %>
<% if(lang==='en'){ %>Latest Cars<% } %>
<% if(lang==='cn'){ %>最新车辆<% } %>
</h2>

<div class="car-list">
<% cars.forEach(car => { %>
<div class="car-item">
<a href="/car/<%= car._id %>?lang=<%= lang %>">
<img src="<%= car.images[0] || '/images/default-car.png' %>" alt="<%= car.name %>">
<h3><%= car.name %></h3>
</a>
<p>
<% if(lang==='lo'){ %>ຍີ່ຫໍ້:<% } %>
<% if(lang==='en'){ %>Brand:<% } %>
<% if(lang==='cn'){ %>品牌:<% } %> <%= car.brand %> | 
Year: <%= car.year %> | Price: $<%= car.price %>
</p>
<span class="status <%= car.status %>"><%= car.status %></span>
</div>
<% }) %>
</div>
</main>

<%- include('partials/footer', { lang: lang }) %>
</body>
</html>